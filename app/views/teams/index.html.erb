<div class="container_center">
  <div class="container_logo">
    <h1>Groupe d'infirmiers</h1>
    <%= link_to "+", new_team_path,class:"btn btn-create" %>
    <% @teams.each do |team| %>
      <%= team.name %>
      <% @members.each do |member| %>
        <%= member.first_name %>
        <%= member.last_name %>
      <% end%>
    <% end%>
  </div>
</div>
